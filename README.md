# Aplicação DevOps

Projeto desenvolvido em Node.js com Express.js.

## Como executar

Instale as dependências:

```bash
npm install
Execute a aplicação:

node src/app.js
Acesse no navegador:

http://localhost:3000

Endpoints
/ — página inicial
/integrantes — lista dos integrantes em JSON